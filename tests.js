const chai = require('chai');
const assert = chai.assert;

const sort = require('./index.js').sort;

describe('sort', function() {
  it('should return an array');
  it('should have as many arrays in the array as keys in the first argument');
});
